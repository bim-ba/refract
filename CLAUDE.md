# refract

> Baseline agent behavior is provided by ai/core (injected each session via the SessionStart hook); add only project-specific rules here.

## Project Overview

<!-- Describe what this project does, its primary language/stack, and any key constraints the agent needs to know about. -->

## Project-Specific Conventions

<!-- Add conventions that override or extend the baseline only where this project differs.
     Examples: custom lint commands, non-standard directory layout, deployment targets,
     domain-specific terminology, or workflow steps specific to this repo. -->
